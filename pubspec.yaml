name: not_defteri_vize
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: "^1.0.4"
  firebase_auth: ^1.1.1
  sqflite: ^2.0.0+3
  get: ^4.1.4
  flutter_staggered_grid_view: ^0.4.0
  google_fonts: ^2.0.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true

  assets:
    - assets/
